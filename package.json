{
	"name": "airbnb-scraper",
	"version": "0.0.1",
	"type": "module",
	"description": "Apify Actor to scrape Airbnb listings using Playwright",
	"engines": {
		"node": ">=18.0.0"
	},
	"scripts": {
		"start": "npm run start:dev",
		"start:prod": "npm run build && node dist/main.js",
		"start:dev": "NODE_OPTIONS='--loader ts-node/esm' node src/main.ts",
		"build": "tsc",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Antigravity",
	"license": "ISC",
	"dependencies": {
		"apify": "^3.2.0",
		"crawlee": "^3.10.0",
		"playwright": "^1.40.0"
	},
	"devDependencies": {
		"@apify/tsconfig": "^0.1.0",
		"@types/node": "^20.0.0",
		"ts-node": "^10.9.0",
		"typescript": "^5.0.0",
		"vitest": "^4.0.18"
	}
}